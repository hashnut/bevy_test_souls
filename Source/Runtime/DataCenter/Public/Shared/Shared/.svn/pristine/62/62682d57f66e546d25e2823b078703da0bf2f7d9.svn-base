#pragma once

#include "SharedEnums.h"

UENUM()
enum class UpdateSignType : uint8				// 현재는 Gold에 한해서 쓰이는 Increase, Decrease 구분용 타입
{
	DECREASE = 0,								// 감소
	INCREASE = 1								// 증가
};
