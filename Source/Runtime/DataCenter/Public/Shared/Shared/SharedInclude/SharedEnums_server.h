#pragma once

#include "SharedEnums.h"

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// 공용으로 쓰이는 enum
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// 증가/감소 타입
enum class UpdateSignType : uint8				// 현재는 Gold, 경험치에 한해서 쓰이는 Increase, Decrease 구분용 타입
{
	DECREASE = 0,								// 감소
	INCREASE = 1								// 증가
};

enum class LevelChangeType : uint8				// Level 변경 타입
{
	NOCHANGE	= 0,							// 변경없음
	UP			= 1,							// 레벨업	
	DOWN		= 2								// 레벨다운
};